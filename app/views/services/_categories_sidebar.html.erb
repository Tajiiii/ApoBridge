<div class="box">
  <%= link_to '一覧に戻る', services_path %>
	<h3>カテゴリー一覧</h3>
	<% categories.each do |category| %>
	<table>
		<td><%= link_to category.name, category_path(category.id) %></td>
	</table>
	<% end %>
</div>
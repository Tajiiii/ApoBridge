<h2 class="text-center">通知</h2>
<div class="destroy-all" style="text-align: right;">
  <%= link_to '全削除', notification_path(@notifications), method: :delete ,class:"fas fa-trash" %>
</div>
  <hr>
<div class="text-center">
  <% if @notifications.exists? %>
  	<div class="users-index">
  		<%= render @notifications %>
  	</div>
  <% else %>
  	<p>通知はありません</p>
  <% end %>

  <%= paginate @notifications %>
</div>
<div class="row">

	<h3>登録情報</h3>
	<div class="col-lg-4">
		<%= render 'profile', user: @user %>
	</div>
	<div class="col-lg-8">
		<h3>投稿一覧</h3>
		<table class="table">
				<tr>
					<% @services.each do |service| %>
						<td class="service">
								<%= link_to service_path(service) do %>
									<%= attachment_image_tag service, :image, size: "200x200", fallback: "noimage.jpg", size: "200x200" %><br>
									<%= service.name %><br>
									<%= service.category.name %><br>
								<% end %>
						</td>
					<% end %>
				</tr>
			</table>
			<div class="text-center">
				<%= paginate @services %>
			</div>

			<h3>いいねしたサービス</h3>
			<table class="table">
				<% @user.like_services.each do |service| %>
						<tr>
							<td><%= link_to service_path(service) do %>
										<%= attachment_image_tag service, :image, size: "200x200", fallback: "noimage.jpg", size: "100x100" %>
										<%= service.name %>
									<% end %>
							</td>
						</tr>
				<% end %>
			</table>
			<div class="text-center">
				<%= paginate @like_services %>
			</div>
	</div>
</div>